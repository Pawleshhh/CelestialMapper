<UserControl x:Class="CelestialMapper.UI.CelestialMap"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:CelestialMapper.UI"
             mc:Ignorable="d"
             x:Name="celestialMap"
             d:DesignHeight="450"
             d:DesignWidth="800"
             >
    <Grid x:Name="mainGrid">

        <Ellipse x:Name="ellipseBackground"
                 Height="{DynamicResource Double.Map.Diameter}"
                 Width="{DynamicResource Double.Map.Diameter}"
                 Fill="#050b38"
                 Stroke="#278a7d"
                 />

        <!--  Lines  -->
        <ItemsControl x:Name="lineItems"
                      Height="{DynamicResource Double.Map.Diameter}"
                      Width="{DynamicResource Double.Map.Diameter}"
                      RenderTransformOrigin="0.5, 0.5"
                      />

        <!--<Grid x:Name="linesGrid" RenderTransformOrigin="0.5, 0.5">
            <Grid.RenderTransform>
                <ScaleTransform ScaleX="-1" ScaleY="1" />
            </Grid.RenderTransform>
            <Grid.Resources>
                <Style TargetType="Rectangle">
                    <Setter Property="HorizontalAlignment" Value="Center" />
                    <Setter Property="Fill" Value="#278a7d" />
                    <Setter Property="Stroke" Value="#278a7d" />
                    <Setter Property="StrokeThickness" Value="0.25" />
                    <Setter Property="RenderTransformOrigin" Value="0.5, 0.5" />
                </Style>
            </Grid.Resources>

            <Rectangle />

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="15" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="30" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="45" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="60" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="75" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="90" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="105" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="120" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="135" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="150" />
                </Rectangle.RenderTransform>
            </Rectangle>

            <Rectangle>
                <Rectangle.RenderTransform>
                    <RotateTransform Angle="165" />
                </Rectangle.RenderTransform>
            </Rectangle>

        </Grid>-->
    </Grid>
</UserControl>
